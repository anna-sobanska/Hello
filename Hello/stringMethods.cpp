#include <iostream>
#include <string>
#include <algorithm>

void stringMethods() {
	std::string word = "Ala ma kota";

	//transform(word.begin(), word.end(), word.begin(), ::tolower);

	//size_t position = word.find("a");

	//word.erase(0, 4);

	//word.insert(7, "malego ");

	//word.replace(4, 2, "nie ma");

	//std::string newWord = word.substr(4,2);
	std::cout << std::endl << word << std::endl;
}